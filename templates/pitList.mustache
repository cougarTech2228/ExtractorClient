{{> header}}
<div class="container">
    <div class="row">
        <div class="col s12">
            <h1>Pit List</h1>

            <ul class="collection with-header">
                <li class="collection-item">
                    <div>Pit Submission
                        <div class="secondary-content">Team</div>
                    </div>
                </li>
                {{#matches}}
                    <li class="collection-item"><a href="{{BASEURI}}pit/{{teamNum}}">
                        <div>Pit Data
                            <div class="secondary-content">{{teamNum}}</div>
                        </div>
                    </a></li>
                {{/matches}}
            </ul>
        </div>
    </div>
</div>
{{> footer}}
